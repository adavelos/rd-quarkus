package org.argonath.rd.entry.test;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends EntryResourceTest {

    // Execute the same tests but in native mode.
}